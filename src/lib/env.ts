// Validación de variables de entorno del servidor (no expuestas al cliente)
export const serverEnv = {
  // Agregar aquí conforme se necesiten
} as const;

// Variables públicas (seguras para el cliente)
export const publicEnv = {
  // Agregar aquí conforme se necesiten
} as const;
